<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Students by Course</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- External Stylesheets -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

    <!-- Custom CSS -->
    <style>
        body {
            background: linear-gradient(135deg, #2C3E50, #fafafa);
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }
        
        .container {
            margin-top: 30px;
        }

        .card {
            background-color: white;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        .table th {
            background-color: #2C3E50;
            color: white;
        }

        h1 {
            color: #2C3E50;
            text-align: center;
        }

        a {
            color: #007bff;
            text-decoration: none;
        }
        
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>Students by Course</h1>

            <% if (students && students.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover mt-4">
                        <thead>
                            <tr>
                                <th>Student Number</th>
                                <th>First Name</th>
                                <th>Last Name</th>
                                <th>Email</th>
                                <th>Course</th>
                                <th>TA</th>
                                <th>Address (Street)</th>
                                <th>Address (City)</th>
                                <th>Address (Province)</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% students.forEach(student => { %>
                                <tr>
                                    <td><a href="/studentByNum?studentNum=<%= student.studentNum %>"><%= student.studentNum %></a></td>
                                    <td><%= student.firstName %></td>
                                    <td><%= student.lastName %></td>
                                    <td><%= student.email %></td>
                                    <td><%= student.course %></td>
                                    <td><%= student.TA ? "Yes" : "No" %></td>
                                    <td><%= student.addressStreet || "N/A" %></td>
                                    <td><%= student.addressCity || "N/A" %></td>
                                    <td><%= student.addressProvince || "N/A" %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-warning" role="alert">
                    <%= message || "No students found for the specified course." %>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
